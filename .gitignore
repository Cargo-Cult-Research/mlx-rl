# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Environments
.venv/
venv/
.env

# Training artifacts / run outputs
runs/
adapters/
checkpoints/
fits/
wandb/
*.safetensors
*.npz
*.jsonl
# curated eval sets under data/ are source, not run artifacts
!data/*.jsonl
# difficulty labels are measured once and reused forever — source, not scratch
!data/labels/*.jsonl
# captured search results cost real money to fetch and are frozen on purpose;
# regenerating them is a metered API call, not a re-run — source, not scratch
!data/serps/*.jsonl

# OS
.DS_Store
demo/flags.jsonl
data/deepcoder/
scripts/.rl-dash-public-until
runs/human-review/labels.jsonl
