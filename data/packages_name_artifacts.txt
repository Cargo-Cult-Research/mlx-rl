# Names the reply-scanner flags as "not on PyPI" that are NOT invented packages.
# Reviewed by hand 2026-08-18. No syntactic rule separates an invented package
# name from a real identifier that belongs to something else, so this list is a
# judgment call and is kept in the open. The install-command-only metric
# (missing_install) needs none of this and is the conservative headline.
#
# system packages (apt/brew, not pip)
git
mpirun
glib2-devel
gobject-introspection
# C / stdlib function names
pam_start
pam_end
pam_authenticate
redirect_stderr
# an exception, not a package
syntaxerror
# Airflow operator/hook classes inside apache-airflow
slackwebhookoperator
slackapipostoperator
slackwebhookhook
# darts model classes, not distributions
arimamodel
lightgbmmodel
nbeatsmodel
# prose words caught by bold/comment scanning
needed
unmaintained
cross-validation
# a JavaScript library, not a PyPI distribution
highcharts
